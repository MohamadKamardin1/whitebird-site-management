/** Coastal Ledger application shell: persistent context, explicit scope, and concise operational navigation. */
import { ReactNode, useState } from "react";
import { Link, useLocation } from "wouter";
import { Bell, ChevronDown, LogOut, Menu, Plus, Search, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuLabel, DropdownMenuSeparator, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { Sheet, SheetContent, SheetTitle, SheetTrigger } from "@/components/ui/sheet";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip";
import { useAuth } from "@/contexts/AuthContext";
import { canSee, primaryNavigation, roleLabels, secondaryNavigation } from "@/lib/navigation";
import { brandMark } from "@/lib/assets";
const LOGO = brandMark;
const isActive = (location: string, href: string) => href === "/" ? location === "/" : location.startsWith(href);
function Navigation({ compact = false }: { compact?: boolean }) { const [location] = useLocation(); const { user } = useAuth(); const itemClass = (active: boolean) => `group flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold transition-all duration-150 ${active ? "bg-white/12 text-white shadow-[inset_3px_0_0_#2BC4B6]" : "text-[#CBD8D6] hover:bg-white/8 hover:text-white"}`; return <nav className={compact ? "mt-7" : "flex min-h-0 flex-1 flex-col"} aria-label="Primary navigation"><p className="ledger-label mb-3 px-3 text-[#8DAEAA]">Operations</p><div className="space-y-1">{primaryNavigation.filter((item) => canSee(user?.role, item.roles)).map((item) => { const Icon = item.icon; return <Link key={item.href} href={item.href} className={itemClass(isActive(location, item.href))}><Icon size={18} strokeWidth={1.9} /><span>{item.label}</span></Link>; })}</div><div className="mt-auto pt-8"><p className="ledger-label mb-3 px-3 text-[#8DAEAA]">Account</p><div className="space-y-1">{secondaryNavigation.map((item) => { const Icon = item.icon; return <Link key={item.href} href={item.href} className={itemClass(isActive(location, item.href))}><Icon size={18} strokeWidth={1.9} /><span>{item.label}</span></Link>; })}</div></div></nav>; }
export function AppShell({ children }: { children: ReactNode }) { const [location, navigate] = useLocation(); const [quickOpen, setQuickOpen] = useState(false); const { user, signOut } = useAuth(); const initials = (user?.full_name || user?.email || "WB").split(/[\s@]+/).filter(Boolean).slice(0, 2).map((part) => part[0]?.toUpperCase()).join(""); const pageName = primaryNavigation.find((item) => isActive(location, item.href))?.label || secondaryNavigation.find((item) => isActive(location, item.href))?.label || "Operations"; return <div className="coastal-shell min-h-screen bg-[#F6F2E9] text-[#132E33]"><aside className="fixed inset-y-0 left-0 z-30 hidden w-[272px] flex-col bg-[#072E35] px-4 py-5 lg:flex"><Link href="/" className="flex items-center gap-3 rounded-xl px-2 py-2 text-white"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-[#F8F5EC] shadow-lg"><img src={LOGO} alt="" className="h-8 w-8 object-contain" /></span><span className="leading-tight"><strong className="block text-[15px] tracking-tight">White Bird</strong><span className="text-xs font-medium text-[#A9C8C3]">Zanzibar operations</span></span></Link><div className="tide-line my-6" /><Navigation /><div className="mt-5 rounded-2xl border border-white/10 bg-white/[0.06] p-3.5 text-xs leading-5 text-[#C4D8D4]"><span className="ledger-label mb-1 block text-[#87B9B0]">Your current scope</span><strong className="block text-sm text-white">{roleLabels[user?.role || ""] || "Operations user"}</strong>Your accessible sites and actions are controlled by the White Bird policy layer.</div></aside><main className="min-h-screen lg:pl-[272px]"><header className="sticky top-0 z-20 flex h-[76px] items-center justify-between border-b border-[#DCD6C8]/90 bg-[#F6F2E9]/92 px-4 backdrop-blur-xl sm:px-7 lg:px-10"><div className="flex min-w-0 items-center gap-3"><Sheet><SheetTrigger asChild className="lg:hidden"><Button variant="ghost" size="icon" className="text-[#17383B]"><Menu size={20} /><span className="sr-only">Open navigation</span></Button></SheetTrigger><SheetContent side="left" className="w-[292px] border-0 bg-[#072E35] p-4 text-white"><SheetTitle className="sr-only">White Bird navigation</SheetTitle><Link href="/" className="flex items-center gap-3 px-2 py-2"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-[#F8F5EC]"><img src={LOGO} alt="" className="h-8 w-8" /></span><span><strong className="block">White Bird</strong><span className="text-xs text-[#A9C8C3]">Zanzibar operations</span></span></Link><Navigation compact /></SheetContent></Sheet><div className="min-w-0"><p className="ledger-label text-[#6E7771]">White Bird Zanzibar</p><h1 className="truncate font-serif text-2xl font-medium tracking-[-0.02em] text-[#17383B]">{pageName}</h1></div></div><div className="flex items-center gap-1.5 sm:gap-3"><Tooltip><TooltipTrigger asChild><Button variant="ghost" size="icon" className="hidden text-[#33555A] sm:inline-flex" onClick={() => navigate("/operations/issues")}><Search size={19} /><span className="sr-only">Search operations</span></Button></TooltipTrigger><TooltipContent>Search operations</TooltipContent></Tooltip><Button className="hidden bg-[#0F7667] text-white shadow-sm hover:bg-[#0B6155] sm:inline-flex" onClick={() => setQuickOpen(!quickOpen)}><Plus size={17} /> Quick action</Button><Tooltip><TooltipTrigger asChild><Button variant="ghost" size="icon" className="relative text-[#33555A]" onClick={() => navigate("/notifications")}><Bell size={19} /><span className="absolute right-2.5 top-2.5 h-2 w-2 rounded-full bg-[#D17837] ring-2 ring-[#F6F2E9]" /><span className="sr-only">Notifications</span></Button></TooltipTrigger><TooltipContent>Notifications</TooltipContent></Tooltip><DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" className="h-auto gap-2 rounded-xl px-1.5 py-1.5 hover:bg-[#E9E3D6]"><Avatar className="h-9 w-9 border border-[#D7D2C5] bg-[#DDEAE6]"><AvatarFallback className="bg-[#DDEAE6] text-xs font-bold text-[#0F5D54]">{initials}</AvatarFallback></Avatar><ChevronDown size={15} className="hidden text-[#587075] sm:block" /></Button></DropdownMenuTrigger><DropdownMenuContent align="end" className="w-64"><DropdownMenuLabel><span className="block truncate">{user?.full_name || user?.email}</span><span className="mt-1 block text-xs font-normal text-muted-foreground">{roleLabels[user?.role || ""] || "White Bird user"}</span></DropdownMenuLabel><DropdownMenuSeparator /><DropdownMenuItem onClick={() => navigate("/settings/profile")}>Account & security</DropdownMenuItem><DropdownMenuItem onClick={() => navigate("/notifications")}>Notifications</DropdownMenuItem><DropdownMenuSeparator /><DropdownMenuItem className="text-red-700 focus:text-red-700" onClick={() => void signOut()}><LogOut size={16} /> Sign out</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div></header>{quickOpen && <div className="fixed right-4 top-[84px] z-40 w-[min(360px,calc(100vw-2rem))] rounded-2xl border border-[#D9D3C6] bg-[#FFFDF8] p-4 shadow-[0_24px_70px_rgba(7,46,53,.18)] sm:right-7 lg:right-10"><div className="flex items-center justify-between"><div><p className="ledger-label text-[#0F7667]">Fast track</p><h2 className="font-serif text-xl">Start an authorised task</h2></div><Button variant="ghost" size="icon" onClick={() => setQuickOpen(false)}><X size={18} /></Button></div><div className="mt-4 grid gap-2"><Button variant="outline" className="justify-start bg-white" onClick={() => navigate("/attendance")}>Record today’s attendance</Button><Button variant="outline" className="justify-start bg-white" onClick={() => navigate("/operations/issues")}>Raise an issue or job</Button><Button variant="outline" className="justify-start bg-white" onClick={() => navigate("/inspections")}>Start an inspection</Button></div></div>}<section className="mx-auto w-full max-w-[1680px] px-4 py-6 sm:px-7 sm:py-8 lg:px-10">{children}</section></main></div>; }
