import { useEffect, useState } from "react";
import { ArrowDownToLine, ArrowRight, FileSpreadsheet, Loader2, Save, Settings2, ShieldCheck, UploadCloud, UsersRound, Warehouse } from "lucide-react";
import { Link } from "wouter";
import { AppShell } from "@/components/AppShell";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { ApiErrorPanel } from "@/components/WorkspacePrimitives";
import { useLanguage } from "@/contexts/LanguageContext";
import { api, asPaginated } from "@/lib/api";
import { isIndividualCleanerRegistrationReady } from "@/lib/cleanerRegistration";

type Mode = "hr" | "store" | "admin";
type Site = { id: number; name: string; code?: string; is_active?: boolean };
const roleCopy: Record<Mode, { eyebrow: string; title: string; description: string }> = {
  hr: { eyebrow: "People operations", title: "A workforce register with a clear next step for every cleaner.", description: "Choose the right onboarding path, site, and employment status before any cleaner record is created." },
  store: { eyebrow: "Supply operations", title: "Keep every configured store ready for the workday.", description: "Review reorder risks, stock requests, fulfillment handovers, and movement evidence without mixing inventory ownership into site execution." },
  admin: { eyebrow: "Platform administration", title: "Configure the operating estate without entering daily site work.", description: "Manage users, roles, sites, zones, shifts, permission governance, and audit controls from a dedicated administration boundary." },
};
const fieldClass = "h-10 border-[#D8D2C5] bg-white";
const selectClass = fieldClass + " w-full rounded-md px-3 text-sm text-[#294A4D]";

function Field({ label, required = false, children }: { label: string; required?: boolean; children: React.ReactNode }) {
  return <label className="grid gap-2"><Label>{label}{required ? <span className="ml-1 text-[#B14B35]">*</span> : null}</Label>{children}</label>;
}

function useActiveSites() {
  const [sites, setSites] = useState<Site[]>([]);
  const [error, setError] = useState<unknown>(null);
  useEffect(() => { void api.get("/sites?page_size=100").then((result) => setSites((asPaginated(result).results as Site[]).filter((site) => site.is_active !== false))).catch(setError); }, []);
  return { sites, siteError: error };
}

function StatusSiteSelector({ status, siteId, onStatusChange, onSiteChange, sites, t }: { status: string; siteId: string; onStatusChange: (value: string) => void; onSiteChange: (value: string) => void; sites: Site[]; t: (key: string) => string }) {
  return <div className="grid gap-4 sm:grid-cols-2"><Field label={t("Onboarding status")} required><select className={selectClass} value={status} onChange={(event) => onStatusChange(event.target.value)}><option value="trainee">{t("Trainee")}</option><option value="active">{t("Active cleaner")}</option></select></Field><Field label={t("Assigned site")} required><select className={selectClass} value={siteId} onChange={(event) => onSiteChange(event.target.value)}><option value="">{t("Select site")}</option>{sites.map((site) => <option key={site.id} value={site.id}>{site.name}{site.code ? ` · ${site.code}` : ""}</option>)}</select></Field></div>;
}

function IndividualCleanerRegistration() {
  const { t } = useLanguage(); const { sites, siteError } = useActiveSites();
  const blank = { first_name: "", last_name: "", id_type: "nida", id_number: "", birth_date: "", gender: "unspecified", living_location: "", phone_number: "", near_person_name: "", near_person_relationship: "", near_person_phone: "", notes: "", onboarding_status: "trainee", site_id: "" };
  const [form, setForm] = useState(blank); const [saving, setSaving] = useState(false); const [error, setError] = useState<unknown>(null); const [message, setMessage] = useState("");
  const update = (key: keyof typeof blank, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const canSave = isIndividualCleanerRegistrationReady(form) && Boolean(form.site_id);
  async function submit() { setSaving(true); setError(null); setMessage(""); try { await api.post("/cleaners", { ...form, site_id: Number(form.site_id) }); setForm(blank); setMessage(t("Cleaner registered with the selected status and site.")); } catch (caught) { setError(caught); } finally { setSaving(false); } }
  return <section className="rounded-2xl border border-[#DDD7CA] bg-[#FFFDF8] p-5 sm:p-6"><div className="flex items-start gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#E5F1ED] text-[#0F7667]"><UsersRound size={20} /></span><div><p className="ledger-label text-[#0F7667]">{t("Individual onboarding")}</p><h2 className="font-serif text-2xl text-[#21464A]">{t("Register one cleaner")}</h2><p className="mt-2 text-sm leading-6 text-[#687874]">{t("Choose whether this is an existing Active cleaner or a new Trainee, then assign the destination site before saving.")}</p></div></div>{siteError || error ? <div className="mt-4"><ApiErrorPanel error={siteError || error} onRetry={() => setError(null)} /></div> : null}{message ? <p className="mt-4 rounded-xl bg-[#E5F0EC] px-4 py-3 text-sm text-[#175F55]">{message}</p> : null}<div className="mt-5 grid gap-4"><StatusSiteSelector status={form.onboarding_status} siteId={form.site_id} onStatusChange={(value) => update("onboarding_status", value)} onSiteChange={(value) => update("site_id", value)} sites={sites} t={t} />{form.onboarding_status === "active" ? <p className="rounded-lg bg-[#FFF4D8] px-3 py-2 text-sm text-[#805B18]">{t("Use Active only for an existing verified worker. Trainee starts a 90-day trainee programme automatically.")}</p> : null}<div className="grid gap-4 sm:grid-cols-2"><Field label={t("First name")} required><Input className={fieldClass} value={form.first_name} onChange={(event) => update("first_name", event.target.value)} /></Field><Field label={t("Last name")} required><Input className={fieldClass} value={form.last_name} onChange={(event) => update("last_name", event.target.value)} /></Field><Field label={t("ID type")} required><select className={selectClass} value={form.id_type} onChange={(event) => update("id_type", event.target.value)}><option value="nida">NIDA</option><option value="zanzibar_id">Zanzibar ID</option><option value="birth_certificate">Birth certificate</option></select></Field><Field label={t("ID number")} required><Input className={fieldClass} value={form.id_number} onChange={(event) => update("id_number", event.target.value)} /></Field><Field label={t("Birth date")} required><Input className={fieldClass} type="date" value={form.birth_date} onChange={(event) => update("birth_date", event.target.value)} /></Field><Field label={t("Gender")}><select className={selectClass} value={form.gender} onChange={(event) => update("gender", event.target.value)}><option value="unspecified">{t("Not specified")}</option><option value="female">{t("Female")}</option><option value="male">{t("Male")}</option><option value="other">{t("Other")}</option></select></Field><Field label={t("Living location")}><Input className={fieldClass} value={form.living_location} onChange={(event) => update("living_location", event.target.value)} /></Field><Field label={t("Phone number")}><Input className={fieldClass} value={form.phone_number} onChange={(event) => update("phone_number", event.target.value)} placeholder="+255..." /></Field></div></div><div className="mt-5 border-t border-[#E5DED3] pt-5"><p className="ledger-label text-[#71807A]">{t("Emergency contact")}</p><div className="mt-3 grid gap-4 sm:grid-cols-3"><Field label={t("Contact name")}><Input className={fieldClass} value={form.near_person_name} onChange={(event) => update("near_person_name", event.target.value)} /></Field><Field label={t("Relationship")}><Input className={fieldClass} value={form.near_person_relationship} onChange={(event) => update("near_person_relationship", event.target.value)} /></Field><Field label={t("Contact phone")}><Input className={fieldClass} value={form.near_person_phone} onChange={(event) => update("near_person_phone", event.target.value)} placeholder="+255..." /></Field></div></div><div className="mt-5"><Field label={t("Notes")}><Textarea className="min-h-20 bg-white" value={form.notes} onChange={(event) => update("notes", event.target.value)} /></Field></div><Button disabled={!canSave || saving} className="mt-5 bg-[#0F7667] text-white hover:bg-[#0B6155]" onClick={() => void submit()}>{saving ? <Loader2 className="animate-spin" size={16} /> : <Save size={16} />}{t("Register cleaner")}</Button></section>;
}

function BulkWorkbookOnboarding() {
  const { t } = useLanguage(); const { sites, siteError } = useActiveSites();
  const [onboardingStatus, setOnboardingStatus] = useState("trainee"); const [siteId, setSiteId] = useState(""); const [file, setFile] = useState<File | null>(null); const [preview, setPreview] = useState<Record<string, unknown> | null>(null); const [busy, setBusy] = useState(false); const [error, setError] = useState<unknown>(null);
  const configured = Boolean(siteId);
  const downloadTemplate = () => { if (!configured) return; window.location.assign(`/api/site-management/v1/hr/cleaners/template.xlsx?onboarding_status=${onboardingStatus}&site_id=${siteId}`); };
  async function previewWorkbook() { if (!file || !configured) return; const form = new FormData(); form.append("workbook", file); setBusy(true); setError(null); try { setPreview(await api.post<Record<string, unknown>>("/hr/cleaners/import/preview", form)); } catch (caught) { setError(caught); } finally { setBusy(false); } }
  async function commitWorkbook() { if (!file || !preview || !configured || Number(preview.rejected_rows || 0) > 0) return; const form = new FormData(); form.append("workbook", file); form.append("onboarding_status", onboardingStatus); form.append("site_id", siteId); setBusy(true); setError(null); try { setPreview(await api.post<Record<string, unknown>>("/hr/cleaners/import", form)); } catch (caught) { setError(caught); } finally { setBusy(false); } }
  return <section className="rounded-2xl border border-[#DDD7CA] bg-[#FFFDF8] p-5 sm:p-6"><div className="flex items-start gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#E5F1ED] text-[#0F7667]"><FileSpreadsheet size={20} /></span><div><p className="ledger-label text-[#0F7667]">{t("Bulk onboarding")}</p><h2 className="font-serif text-2xl text-[#21464A]">{t("Configure and download a template")}</h2><p className="mt-2 text-sm leading-6 text-[#687874]">{t("First choose the onboarding status and one destination site. The workbook omits editable site and status columns because those choices are enforced by the system when you import.")}</p></div></div>{siteError || error ? <div className="mt-4"><ApiErrorPanel error={siteError || error} onRetry={() => setError(null)} /></div> : null}<div className="mt-5 grid gap-4"><StatusSiteSelector status={onboardingStatus} siteId={siteId} onStatusChange={setOnboardingStatus} onSiteChange={(value) => { setSiteId(value); setFile(null); setPreview(null); }} sites={sites} t={t} /><div className="rounded-xl border border-dashed border-[#BBD7CF] bg-[#F4FAF7] p-4"><p className="font-semibold text-[#315156]">{t("Customized template configuration")}</p><p className="mt-1 text-sm text-[#62736E]">{t("The downloaded workbook will show this status and site in its configuration sheet. The import will use the selected values here, not any spreadsheet changes.")}</p><Button disabled={!configured} variant="outline" className="mt-3 bg-white" onClick={downloadTemplate}><ArrowDownToLine size={16} /> {t("Download configured template")}</Button></div><label className="inline-flex h-10 cursor-pointer items-center justify-center gap-2 rounded-md bg-[#0F7667] px-4 text-sm font-semibold text-white hover:bg-[#0B6155] disabled:opacity-50"><UploadCloud size={16} /> {t("Select workbook")}<input disabled={!configured} className="sr-only" type="file" accept=".xlsx" onChange={(event) => { setFile(event.target.files?.[0] || null); setPreview(null); }} /></label>{file ? <div className="rounded-xl border border-[#DDE8E2] bg-[#F5FAF7] p-3 text-sm text-[#315B56]">{t("Selected")} <strong>{file.name}</strong><div className="mt-3 flex flex-wrap gap-2"><Button size="sm" variant="outline" className="bg-white" onClick={() => void previewWorkbook()} disabled={busy}>{busy ? <Loader2 className="animate-spin" size={15} /> : <ShieldCheck size={15} />}{t("Preview rows")}</Button><Button size="sm" className="bg-[#0F7667] text-white" onClick={() => void commitWorkbook()} disabled={busy || !preview || Number(preview.rejected_rows || 0) > 0}>{t("Commit accepted rows")}</Button></div></div> : null}{preview ? <div className="grid gap-2 sm:grid-cols-3">{[[t("Rows"), preview.total_rows], [t("Accepted"), preview.accepted_rows], [t("Rejected"), preview.rejected_rows]].map(([label, value]) => <div className="rounded-lg border border-[#E5DED3] bg-[#FAF8F2] p-3" key={String(label)}><p className="ledger-label text-[#71807A]">{String(label)}</p><p className="mt-1 font-serif text-2xl text-[#21464A]">{String(value)}</p></div>)}</div> : null}</div></section>;
}

export default function RoleWorkspacesPage({ mode }: { mode: Mode }) {
  const { t } = useLanguage(); const copy = roleCopy[mode];
  return <AppShell><div className="space-y-7"><div><p className="ledger-label text-[#0F7667]">{t(copy.eyebrow)}</p><h1 className="mt-2 max-w-3xl font-serif text-4xl tracking-[-.04em] text-[#173B3E]">{t(copy.title)}</h1><p className="mt-3 max-w-3xl text-sm leading-7 text-[#60736E]">{t(copy.description)}</p></div>{mode === "hr" && <><div className="grid gap-5 xl:grid-cols-2"><IndividualCleanerRegistration /><BulkWorkbookOnboarding /></div><section className="rounded-2xl border border-[#DDD7CA] bg-[#0B3540] p-5 text-white sm:p-6"><p className="ledger-label text-[#82C8BC]">{t("HR queue")}</p><h2 className="mt-1 font-serif text-2xl">{t("Move people through the right gate.")}</h2><div className="mt-5 grid gap-2 md:grid-cols-3"><Link href="/hr/people" className="flex items-center justify-between rounded-lg bg-white/[.08] p-3 text-sm"><span className="flex items-center gap-2"><UsersRound size={16} /> {t("People registry")}</span><ArrowRight size={15} /></Link><Link href="/hr/assignments" className="flex items-center justify-between rounded-lg bg-white/[.08] p-3 text-sm"><span>{t("Assignments and shifts")}</span><ArrowRight size={15} /></Link><Link href="/trainees" className="flex items-center justify-between rounded-lg bg-white/[.08] p-3 text-sm"><span>{t("Trainee qualification queue")}</span><ArrowRight size={15} /></Link></div></section></>}{mode === "store" && <div className="grid gap-5 md:grid-cols-3"><Link href="/stores" className="rounded-2xl border border-[#DDD7CA] bg-[#FFFDF8] p-5 hover:border-[#9ECFC4]"><Warehouse className="text-[#0F7667]" /><h2 className="mt-4 font-serif text-2xl text-[#21464A]">{t("Inventory register")}</h2><p className="mt-2 text-sm leading-6 text-[#687874]">{t("Review configured stores, item thresholds, counts, and movement history.")}</p></Link><Link href="/stores" className="rounded-2xl border border-[#DDD7CA] bg-[#FFFDF8] p-5 hover:border-[#9ECFC4]"><ShieldCheck className="text-[#0F7667]" /><h2 className="mt-4 font-serif text-2xl text-[#21464A]">{t("Request review")}</h2><p className="mt-2 text-sm leading-6 text-[#687874]">{t("Process site requests, approve quantities, and keep handovers visible.")}</p></Link><Link href="/reports" className="rounded-2xl border border-[#DDD7CA] bg-[#FFFDF8] p-5 hover:border-[#9ECFC4]"><ArrowRight className="text-[#0F7667]" /><h2 className="mt-4 font-serif text-2xl text-[#21464A]">{t("Supply reporting")}</h2><p className="mt-2 text-sm leading-6 text-[#687874]">{t("See stock risk and fulfillment evidence in the reporting chain.")}</p></Link></div>}{mode === "admin" && <div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{[["Users & roles", "/admin/users", "Create, revoke, and assign platform roles."], ["Sites & zones", "/sites", "Configure the operating estate and zone relationships."], ["Shifts", "/sites", "Define site working modes and shift rules."], ["Audit and reports", "/reports", "Review governance evidence and handover status."], ["Integration settings", "/admin/integrations", "Securely configure DeepSeek and Mapbox for the platform."]].map(([label, href, description]) => <Link href={href} key={label} className="rounded-2xl border border-[#DDD7CA] bg-[#FFFDF8] p-5 hover:border-[#9ECFC4]"><Settings2 className="text-[#0F7667]" /><h2 className="mt-4 font-serif text-2xl text-[#21464A]">{t(label)}</h2><p className="mt-2 text-sm leading-6 text-[#687874]">{t(description)}</p></Link>)}</div>}</div></AppShell>;
}
